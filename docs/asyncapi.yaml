asyncapi: 2.4.0
info:
  title: Async API
  description: API de eventos assíncronos do processamento de ordens do microsserviço order-management-orders
  version: 1.0.0

defaultContentType:
  application/json

servers:
  rabbitmq:
    url: localhost
    protocol: amqp

channels:
  created_order_routing_key:
    publish:
      message:
        name: created_order
        title: Evento de ordem criada
        description: Evento que informa que uma ordem foi criada para que os serviços interessados possam consumir
        payload:
          contentType: application/json
          $ref: "#/components/schemas/OrderCreatedDto"
  
  processed_payment_routing_key:
    subscribe:
      message:
        name: processed_payment
        title: Evento de pagamento processado
        description: Evento de processamento do pagamento para atualização do status da ordem.
        payload:
          contentType: application/json
          $ref: "#/components/schemas/OrderUpdatedDto"
        bindings:
          amqp:
            is: processed_payment_routing_key
            queue:
              name: order_management_orders_processed_payment_queue
              durable: true
              exclusive: false
              autoDelete: false
            retry_queue:
              name: order_management_orders_update_order_retry_queue
              durable: true
              exclusive: false
              autoDelete: false
            dead_queue:
              name: order_management_orders_update_order_dead_queue
              durable: true
              exclusive: false
              autoDelete: false
            exchange:
              name: order_management_events
              type: topic
              durable: true
              autoDelete: false
      

components:
  schemas:
    OrderCreatedDto:
      type: object
      required: 
        - orderId
        - amount
      properties:
        orderId:
          type: integer
          description: Identificador de ordem criada
          example: 1234
        amount: 
          type: number
          description: Valor total da ordem
          example: 250.25

    OrderUpdatedDto:
      type: object
      required: 
        - orderId
        - status
        - attemptedPaymentAt
      properties:
        orderId:
          type: integer
          description: Identificador de ordem criada
          example: 1234
        status: 
          type: string
          description: Status da ordem
          examples: 
            - PROCESSED
            - REJECTED
        attemptedPaymentAt: 
          type: string
          format: date-time
          description: Data de processamento do pagamento
          example: '2024-11-01T19:35:41.00'
